#include "cpu.h"

void Cpu::step()
{
    char buf[1024];

    Cpu::traceBefore(buf, sizeof(buf));
    uint8_t op = fetch8();
    OpInfo op_info = cpu_op_table[op];

    bool result;
    switch (op_info.op_name)
    {
{{dispatch_cpp}}    default:
        result = false;
    }

    Cpu::traceAfter(buf);

    if (!result)
    {
        unimplemented(op);
    }
}