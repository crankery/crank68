#include "cpu/cpu.h"
#include "cpu_defs.g.h"

bool Cpu::dispatch(uint8_t op, OpInfo op_info)
{
    bool result;
    op_names op_name = op_info.op_name;

    switch (op_name)
    {
{{dispatch_cpp}}
    default:
        result = false;
    }

    return result;
}